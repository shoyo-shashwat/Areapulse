flask==3.0.0
flask-cors==4.0.0
groq>=0.7.0
firebase-admin==6.5.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Neon Postgres (new)
psycopg[binary]>=3.1
psycopg-pool>=3.2

# Twilio WhatsApp (you already use these — ensure pinned)
twilio>=9.0.0
requests>=2.31.0
