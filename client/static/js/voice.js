'use strict';
// Voice stub — replace with full voice.js from your original project
window.AreaPulseVoiceAssistant = class {
  constructor(role, cb) { this.role = role; this.cb = cb; }
  setVoiceMode(v) { console.log('[Voice] mode:', v); }
};
